# browser-skill-owui — Feature service
#
# Mode modulaire (socle lancé séparément) :
#   docker compose up -d

services:
  browser-use:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - owui-net
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/healthz').raise_for_status()"]
      interval: 30s
      timeout: 5s
      retries: 3

networks:
  owui-net:
    external: true
